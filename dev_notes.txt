default isadmin is false... for admin panel register alone parse a is_admin=true response from frontend

Hi just learned that this package is out of version but now worries through this command you can properly run the installation using npm:
npm i react-awesome-loaders  --ignore-scripts
and then
npm rebuild --verbose sharp



📌 Flowchart of Admin API Operations

        ┌──────────────┐
        │ Admin Sends  │
        │  Request     │
        └──────┬───────┘
               │
   ┌──────────▼───────────┐
   │  Check if Admin User │
   └──────────┬───────────┘
              │
      ┌──────▼───────┐
      │  If Not Admin│
      │  Return 403  │
      └──────────────┘
              │
    ┌────────▼────────┐
    │  Check If Data  │
    └───────┬────────┘
            │
 ┌─────────▼───────────┐
 │   Add / Update?     │
 │ (`update_or_create`)│
 └─────────┬──────────┘
           │
 ┌────────▼──────────┐
 │   Delete?         │
 │ (`get().delete()`)│
 └───────────────────┘


✅ Step 3: Ensure API Requests Use the Correct Backend URL
Update API requests in React (src/api/api.js or directly inside useEffect functions).

🔹 Update Axios Configuration
If using Axios, set global defaults in src/api/api.js:

javascript
Copy
Edit
import axios from "axios";

const API = axios.create({
  baseURL: "http://127.0.0.1:8000",  // 🔥 Backend URL
  withCredentials: true, // 🔥 Ensures session cookies are sent
});

export default API;
Now, update all API calls to use this:

🔹 Example of API Call in Tips.jsx
Change:

javascript
Copy
Edit
axios.get("http://127.0.0.1:8000/api/tips")
To:

javascript
Copy
Edit
import API from "../api/api"; // Import configured Axios instance

API.get("/api/tips")
✅ Ensures every request correctly includes session cookies.